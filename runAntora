 #!/usr/bin/bash

 #git clone https://github.com/Julie-b35/wiki-antora.git docs &&  cd "$(basename $_ .git)"

 docker run -u $(id -u) -v $PWD:/antora:Z --rm -t antora/antora --cache-dir=./.cache/antora antora-playbook.yml